# Lesson 3: Conditionals

# TODO 1: Basic if condition:
# - Create a variable `x = 10`.
# - Write an `if` statement to check if `x > 5` and print "x is greater than 5".

# TODO 2: if-else statement:
# - Create a variable `age = 20`.
# - Write an `if-else` to print "Adult" if `age >= 18`, otherwise print "Minor".

# TODO 3: if-elif-else ladder:
# - Create a variable `score = 75`.
# - Write an `if-elif-else` statement to classify scores:
#   - `score >= 90`: Print "Excellent".
#   - `score >= 75`: Print "Good".
#   - `score >= 50`: Print "Pass".
#   - Otherwise: Print "Fail".

# TODO 4: Logical operators in conditionals:
# - Create variables `a = True` and `b = False`.
# - Write an `if` statement using `and`, `or`, and `not` operators to print "Condition met" if true.

# TODO 5: Nested if:
# - Create a variable `temperature = 30`.
# - Write nested `if` conditions to print:
#   - "Hot" if `temperature > 25`.
#   - "Warm" if `temperature > 15` but not greater than 25.
#   - Otherwise: Print "Cold".

# TODO 6: Ternary conditional operator:
# - Create a variable `is_raining = True`.
# - Write a single line to print "Take an umbrella" if `is_raining`, otherwise print "No umbrella needed".

# TODO 7: Complex conditionals:
# - Create a variable `number = 23`.
# - Write a conditional to check if `number` is:
#   - Even and greater than 10.
#   - Odd and less than 30.
# - Print the corresponding message.

# TODO 8: Checking membership:
# - Create a list `fruits = ['apple', 'banana', 'cherry']`.
# - Use an `if` statement to check if 'banana' is in the list and print "Banana is available".

# TODO 9: Conditional with input:
# - Ask the user to input their age.
# - Print "You are eligible to vote" if age is 18 or older, otherwise print "Not eligible".

# TODO 10: Debugging a conditional:
# - Write a faulty `if` condition.
# - Use print statements to debug and correct the logic.
